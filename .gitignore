# Ignore all root files/directories by default
/*

# Whitelist allowed directories and files
!/client/
!/server/
!/.gitignore
!/README.md
!/firebase.json
!/firestore.rules
!/.firebaserc

# Explicitly exclude JVM crash logs and scratch files (belt-and-suspenders)
*.log
payload*.json
